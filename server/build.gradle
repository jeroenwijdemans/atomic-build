apply plugin: 'java'
apply plugin: 'application'
apply plugin: 'executable-jar'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'net.nisgits.gradle:gradle-executable-jar-plugin:master-REL-6'
    }
}

project.ext.mainClass = 'atomic.server.Runner'
